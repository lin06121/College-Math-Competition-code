# CLAUDE.md

本文件为 Claude Code 在本仓库工作时的指引。

## 项目概述

**2026 年高教社杯全国大学生数学建模竞赛 C 题：微网与外部电网电力调控策略。**

微网包含光伏发电、小区负载、储能设备，并与外部电网交互购电。目标是在满足小区负载的前提下，制定购电策略与储能充放电策略，使购电费用尽可能低。储能可在电价低或光伏有剩余时充电、电价高时放电。

四个子问题：
- **问题 1**：电价、负载按典型日固定，给定光伏预测，制定每日 0:00 计划购电策略（要求 0:00 与 24:00 储电量相同）。
- **问题 2**：电价固定，负载与光伏逐时变化；计划购电不足时触发**紧急购电，电价为当时电价的 5 倍**。
- **问题 3**：每天 0:00 / 6:00 / 12:00 / 18:00 获得未来 24 小时光伏预报；可计划 + 调整。计划量高于调整量的部分按当时电价 **50%** 计违约，调整量高于计划量的超出部分按 **1.5 倍**计。
- **问题 4**：电价实时波动（附件 4），重新计算问题 2、3。

## 环境与命令

> **默认 `python` 不可用**（`D:\devtool\python\python.exe` 只有标准库）。所有数据处理与求解脚本必须使用 Anaconda 解释器。

```bash
PY="/d/devtool/anaconda/conda/python.exe"     # D:\devtool\anaconda\conda\python.exe, Python 3.13.9

# 中文输出在 Windows 控制台会乱码，务必带上 UTF-8 环境变量
PYTHONUTF8=1 "$PY" code/xxx.py
```

已安装：`numpy 2.3.5`、`pandas 2.3.3`、`scipy 1.16.3`、`scikit-learn 1.7.2`、`xgboost 3.4.1`、`matplotlib 3.10.6`、`seaborn 0.13.2`、`openpyxl 3.1.5`。

**未安装任何专用优化求解器**（无 gurobipy / pulp / cvxpy / ortools）。MILP 请用
`scipy.optimize.milp`（HiGHS 后端）配合 `LinearConstraint` / `Bounds(..., integrality=...)`。
若确需 Gurobi，先确认环境再安装，不要假设其可用。

LaTeX：TeXLive 2026（`xelatex` / `pdflatex` 已在 PATH）。`passage/cumcmthesis.cls` 基于 ctex，用 **xelatex** 编译。

## 目录结构

| 路径 | 用途 |
|---|---|
| [code/](code/) | 求解与数据处理代码（当前为空，新脚本放这里） |
| [passage/](passage/) | LaTeX 论文；模板 `cumcmthesis.cls`，样例 `example.tex`，图片放 `passage/figures/` |
| [problem/C题/](problem/C题/) | 题目 `C题.pdf` 与附件 |
| [problem/C题/附件/附件5/](problem/C题/附件/附件5/) | 结果文件模板（`result1.xlsx` … `result4-3.xlsx`） |
| [CUMCM2026Problems/](CUMCM2026Problems/) | 原始赛题包（A–E 题），与 `problem/` 内容重复 |

- `~$附件*.xlsx` 是 Excel 打开文件时产生的锁文件，**不要提交、不要当作数据读取**。
- 仓库当前没有 `.gitignore`，新增产物时注意不要误提交临时文件。
- `C题.pdf` 有密码保护，Read 工具无法直接打开；可用 `pdftotext -layout` 提取，但中文字形会丢失。题目内容以本文件与用户提供的说明为准。

## 数据格式（附件）

时间粒度统一为 **10 分钟，每天 144 个时段**，`Δt = 1/6 h`，电量 `kWh = 功率(kW) × 1/6`。

| 附件 | 结构 | 字段 |
|---|---|---|
| 附件 1 | `Sheet1`，144 行 × 4 列 | `时间`、`电价`(元/kWh)、`小区负载`(kW)、`光伏发电预测功率`(kW) |
| 附件 2 | 2 个工作表：`小区负载`、`光伏发电实际功率`，365 × 145 | 行索引为日期（2025-01-01 起全年），列为 144 个时段 |
| 附件 3 | `Sheet1`，1460 × 26 | `日期`、`预报时间`(0:00/6:00/12:00/18:00)、`预报1小时` … `预报24小时`(kW) |
| 附件 4 | `Sheet1`，365 × 145 | 行=日期，列=144 时段，值=实时电价(元/kWh) |

附件 2、4 的列标签为 `00:10, 00:20, …, 23:50, 0:00+1`，**表示时段结束时刻**：列 `00:10` 对应 00:00–00:10 这一时段，`0:00+1` 即当天 24:00 / 次日 0:00。

附件 3 注意事项：
- `日期` 列仅在每天首行（0:00）填写，其余 3 行为空，读取时需 **forward-fill**；
- 预报为整点、未来 24 小时（`预报k小时`），需**插值到 10 分钟**再与附件 2、4 对齐。

### 结果模板工作表名（附件 5）

| 文件 | 工作表 |
|---|---|
| `result1.xlsx` | `计划购电量`、`充放电量` |
| `result2.xlsx` | `计划购电量`、`充放电量`、`紧急购电量` |
| `result3.xlsx` | `计划购电量`、`调整购电量`、`充放电量`、`紧急购电量` |
| `result4-2.xlsx` | `计划购电量`、`充放电量`、`紧急购电量` |
| `result4-3.xlsx` | `计划购电量`、`调整购电量`、`充放电量`、`紧急购电量` |

输出时请**复制模板文件再写入**，以保证表头与格式一致。

## 储能参数与约束

| 参数 | 数值 |
|---|---|
| 最大容量 | 12000 kWh |
| 最大充/放电功率 | 5000 kW |
| 储电量允许范围 | 1200 – 10800 kWh |
| 2025-01-01 0:00 初始储电量 | 6000 kWh |
| 充/放电效率 | 90% |

- 储能动态：`E(t+1) = E(t) + η·P_ch(t)·Δt − P_dis(t)/η·Δt`，`η = 0.9`。
- 充放电互斥（0-1 变量 `u(t)`）：`P_ch(t) ≤ 5000·u(t)`，`P_dis(t) ≤ 5000·(1−u(t))`。
- 问题 1 要求 `E(0) = E(144)`。

## 建模要点

统一时间集合 `T = {1,…,144}`，决策变量 `P_grid`、`P_ch`、`P_dis`、`E`、`u`；问题 2–4 另加 `P_plan`、`P_adj`、`P_em`。

功率平衡（核心约束）：
```
P_grid(t) + PV(t) + P_dis(t) = Load(t) + P_ch(t)
```

目标函数：
- 问题 1：`min Σ_t price(t)·P_grid(t)·Δt`
- 问题 2：`min Σ_t price(t)·P_plan(t)·Δt + 5·Σ_t price(t)·P_em(t)·Δt`
- 问题 3：`min 计划费用 + 调整费用 + 5·Σ_t price(t)·P_em(t)·Δt`
  （调整费用：`P_plan > P_adj` 的差额按 `0.5·price`；`P_adj > P_plan` 的超出按 `1.5·price`）
- 问题 4：把 `price(t)` 换成附件 4 实时电价，重算问题 2、3。

问题 1、2 为确定性 MILP；问题 3 为多阶段/滚动优化；问题 4 电价不确定，可考虑鲁棒或随机优化。

## 数据特征（供建模与分析章节参考）

- **负载**：白天高于夜间；周五、周六白天峰值约为工作日的 60%；工作日午间 12:00 前后有小低谷；22:00 后进入低谷。
- **电价**：早、午、晚三峰 + 夜间谷；周五、周六非晚峰约为其他日的一半；工作日午间为谷、周末午间不低；附件 4 实时电价约 0.2–1.7 元/kWh。
- **光伏**：夜间为 0，正午前后峰值；夏季（6–8 月）显著高于冬季；预报（附件 3）与实际（附件 2）存在偏差。

## 交付物

- [ ] `result1.xlsx`、`result2.xlsx`、`result3.xlsx`、`result4-2.xlsx`、`result4-3.xlsx`
- [ ] 竞赛论文（含表 1、表 2、表 3 格式的结果）
- [ ] `code/` 下的源代码（预处理、建模、求解、结果输出）

## 关键约定

- 时间标签 `0:00+1` 表示**次日 0:00**，即当天 24:00。
- 附件 2、4 覆盖 2025 全年；问题 2、3 的结果从 **2025.2.1** 起。
- 论文中需给出的指定日期（表 3）：2025.3.20、2025.6.21、2025.9.23、2025.12.21。
- 表 1 指定时段（10 分钟）：10:00–10:10、12:00–12:10、14:00–14:10、16:00–16:10、18:00–18:10、20:00–20:10，另加全天购电量与全天购电费。
- 表 2 指定时段（4 小时）：0:00–4:00、4:00–8:00、8:00–12:00、12:00–16:00、16:00–20:00、20:00–24:00，另加 0:00、24:00 储电量。
- 单位约定：功率 kW，电量 kWh，电价 元/kWh。
